name: 'Hello World'
pool: 
  vmImage: 'ubuntu-latest'

variables:
  - name: buildSourceVershionShort
    value: $[ format('{0}', variables['Build.SourceVersion']) ]
    readonly: true

stages:
- stage: VariablesOutput
  jobs:
  - job: Echo
    steps:
    - script: |
        echo $(buildSourceVershionShort)
        echo $(Build.SourceVersion)

name: 'Hello World'
pool: 
  vmImage: 'ubuntu-latest'

variables:
  - name: buildSourceVershionShort
    value: $[ format('{0}', variables.Build.SourceVersion.Substring(0, 7)) ]
    readonly: true

stages:
- stage: VariablesOutput
  jobs:
  - job: Echo
    steps:
    - script: |
        echo $(buildSourceVershionShort)
